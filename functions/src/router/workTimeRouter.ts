import * as express from "express";
import * as admin from "firebase-admin";

export const workTimeRouter = express.Router();

// /start - Start Working
workTimeRouter.post("/start", (req, res) => {});
